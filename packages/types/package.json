{
  "name": "@shared/types",
  "version": "0.12.0",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p .",
    "tsc": "echo \"Warning: no type-check required for @shared/types\"",
    "test": "echo \"Warning: no test specified\"",
    "lint": "eslint . --ext .ts,.tsx,.js --max-warnings=0",
    "format": "prettier --check .",
    "format:fix": "prettier --write ."
  },
  "devDependencies": {
    "@types/mongoose": "^5.11.97"
  }
}
